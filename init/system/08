echo "RSA key files are in folder ~/.ssh/ you can see the private key with command cat ~/.ssh/id_rsa
and the public key with cat ~/.ssh/id_rsa.pub
If you dont find your RSA keys you have to generate them with ssh-keygen -t rsa command."