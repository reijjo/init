echo "The exact installed debian version can be found in folder /etc in file called debian_version.
command 'cat /etc/debian_version'
You can also find debian version in file called os-release in /etc folder.
command 'cat /etc/os-release'"
echo "But I like this command: 'cat /etc/os-release | grep 'PRETTY' | cut -d = -f 2'"